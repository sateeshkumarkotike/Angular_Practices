import { DynamicLoadingDirective } from './dynamic-loading.directive';

describe('DynamicLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
